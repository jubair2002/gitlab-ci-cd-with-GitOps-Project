.survey_service: &survey_service_rules
  rules:
    - changes:
        - survey-service/**/*
      when: always
    - when: never

stages:
  - build
  - push
  - update-manifest

variables:
  SERVICE_NAME: survey-service
  IMAGES_NAME: jubair2002/survey-service
  IMAGE_TAG: $CI_COMMIT_SHA

build_survey_service:
  stage: build
  <<: *survey_service_rules
  script:
    - echo "Building $SERVICE_NAME Docker image..."
    - docker build -t $IMAGES_NAME:$IMAGE_TAG survey-service

push_survey_service:
  stage: push
  <<: *survey_service_rules
  script:
    - echo "Pushing $SERVICE_NAME Docker image to registry....."
    - echo $DOCKER_PASSWORD | docker login -u $DOCKER_USERNAME --password-stdin
    - docker push $IMAGES_NAME:$IMAGE_TAG

update-manifest:
  stage: update-manifest
  <<: *survey_service_rules
  script:
    - echo "Updating manifest"
    - rm -rf microservice-manifest-repo
    - git clone https://oauth2:${GITLAB_TOKEN}@gitlab.com/public-project8832250/microservice-manifest-repo.git  
    - cd microservice-manifest-repo/k8s-manifests
    - yq eval ".spec.template.spec.containers[0].image = \"$IMAGES_NAME:$IMAGE_TAG\"" -i survey-deploy-svc.yml
    - git config user.email "ci@gitlab.com"
    - git config user.name "GitLab CI"
    - git add survey-deploy-svc.yml
    - git commit -m "Update user service image to $IMAGE_TAG"
    - git push origin main